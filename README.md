# cpp-http11-server
Built an HTTP/1.1 compliant multithreaded web server in C++ using BSD sockets, featuring raw TCP stream parsing, persistent keep-alive connections, request routing, and manual handling of partial recv() reads without external libraries.
